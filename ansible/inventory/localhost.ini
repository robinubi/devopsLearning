# Stage 6: Ansible — Inventory
# Targets localhost so you can practise without a remote server
# Later: replace 'localhost' with a real server IP

[local]
localhost ansible_connection=local

[webservers]
localhost ansible_connection=local
